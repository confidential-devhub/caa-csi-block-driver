apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: caa-block-aws
parameters:
  cloudProvider: aws
  awsRegion: us-east-2
  # Recommended for Peer Pods: pin to the same AZ as CAA's AWS_SUBNET_ID.
  awsAvailabilityZone: us-east-2c
  awsVolumeType: gp3
provisioner: caa-csi-block.csi.confidentialcontainers.io
reclaimPolicy: Delete
volumeBindingMode: Immediate
